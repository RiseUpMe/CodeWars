// JavaScript source code
function doubleChar(str) {
    let doubleChar = "";
    for (let a = 0; a < str.length; a++) {
        doubleChar += str[a] + str[a];// Your code here
    }
    return doubleChar;
}